name: Documentation

on:
  push:
    branches: [documentation]
  pull_request:
    branches: [documentation]

jobs:
  deploy:
    uses: ConductionNL/.github/.github/workflows/documentation.yml@main
    with:
      cname: thematiq.conduction.nl

      docs-hosts: nldesign.conduction.nl,thematiq.conduction.nl