<?xml version="1.0"?>
<ruleset name="nldesign"
    xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 http://pmd.sourceforge.net/ruleset_xml_schema.xsd">
    <description>nldesign — Conduction PHPMD ruleset (single source: conduction/hydra-gates).</description>

    <!-- Deviations from the central ruleset go BELOW this line, each with a
         reason. Everything else comes from conduction/hydra-gates. -->
    <rule ref="vendor/conduction/hydra-gates/quality-config/phpmd.xml">
        <exclude name="ShortVariable"/>
    </rule>

    <!-- ShortVariable: the central allowlist plus five colour-space names.

         g,h,s,p,q added for RGB/HSL colour-space math (DarkPaletteService):
         r,g,b (RGB channels) and h,s,l (hue/saturation/lightness) and p,q
         (the standard HSL->RGB intermediate terms) are universal, idiomatic
         single-letter names in every colour-conversion reference implementation.
         The rule is otherwise unchanged, including `minimum`; the allowlist is a
         strict superset of the central one. -->
    <rule ref="rulesets/naming.xml/ShortVariable">
        <properties>
            <property name="minimum" value="3" />
            <property name="exceptions" value="id,db,qb,op,ui,io,gc,tz,pk,fk,to,ch,a,b,l,v,c,t,r,f,n,k,e,g,h,s,p,q" />
        </properties>
    </rule>
</ruleset>
